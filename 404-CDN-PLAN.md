# 404ページとCDNトップの設計案

サイトにまだ無い2つの面――`mojikumi.jp`の404と`cdn.mojikumi.jp`のトップ――を、
既存のテーマの延長として設計するための案です。

**両方とも実装済みです。** 404は案A（落丁／文言は「この番号のページはありません」）、
CDNは単純HTMLのまま設計し直す案で進めました。以下は判断の記録として残します。

## 現状の確認

| 面 | 状態 |
| --- | --- |
| `mojikumi.jp`の404 | 着手前は**存在せず**、Next.jsの既定の白い「404 This page could not be found.」が出ていた |
| `cdn.mojikumi.jp`のトップ | `apps/cdn/build.mjs`が文字列で書き出す素のHTML。`<p>`2本と`<pre>`1つ、CSSなし |
| CDNの404 | **存在しない**。`/v1/mojikumi.js`のような綴り違いはVercelの既定の404になる |
| CDNのrobots | `index.html`の`<meta name="robots" content="noindex">`のみ。`robots.txt`は無い |

どちらも「無いわけではないが、サイトの他の面と同じ手が入っていない」状態です。
404はブランドが最も雑に見える場所で、CDNトップは**貼ったスクリプトの出所を確かめ
に来た人**が最初に見る場所です。どちらも人が来る前提が薄いようでいて、来たときの
印象が重い面です。

## 共通の前提

新しいデザイン言語は作りません。既存の語彙をそのまま使います。

| 要素 | 使うもの |
| --- | --- |
| 色 | `--primary`（臙脂）、`--bg`（生成り）、`--gold`、`--line` |
| 書体 | 見出しは`--font-display`（Shippori Mincho）、本文とラベルは`--font-ui`（Zen Maru Gothic） |
| ラベル | `.eyebrow`（大文字ラテン＋`letter-spacing: 0.18em`） |
| 番号 | `.index-mark` / `.section-number`（`01`、`02`……） |
| 面 | `.hero-specimen`と同じガラス面（`--glass`、`--radius-card`、`--surface-shadow`） |
| 記号 | 括弧そのもの（`「` `『`）。ヘッダーとフッターのブランドマークと同じ扱い |

語彙も揃えます。サイト全体が出版の言い回し（約物、和欧混植、組み見本）で書かれて
いるので、404も**組版の言葉**で説明します。「ページが見つかりません」ではなく、
落丁・ノンブルの語彙です。これはこのサイトでだけ自然に成立する言い方で、他所から
借りてきたイラストや顔文字よりずっと安く、ずっとこのプロダクトらしくなります。

---

## 1. `mojikumi.jp`の404

### 先に効いてくる技術的な制約

案を選ぶ前に、静的書き出しとルートグループの都合で決まってしまう部分があります。

1. **`not-found.tsx`ではなく`global-not-found.tsx`を使う。**
   このサイトはルートグループごとに`<html>`を持つレイアウトが2つあります
   （[app/(ja)/layout.tsx](apps/web/app/(ja)/layout.tsx)と
   [app/(en)/layout.tsx](apps/web/app/(en)/layout.tsx)）。Next.jsは未マッチのURL
   に対してどちらを使うか決められません。

   当初はルート直下の`not-found.tsx`が**何にも包まれない**と読んでいましたが、
   実際に書き出して確認したところ**違いました**。Next.jsは`<html><body>`だけの
   既定レイアウトを差し込むため、`RootDocument`を呼ぶと`<html>`が入れ子になった
   404.htmlが出ます。かといって既定レイアウトに任せると、`<html>`にフォントの
   クラスが付かず、`--font-display`などが解決しません。

   → Next.js 16.2の`app/global-not-found.tsx`がこの場合のための規約です。ルート
   レイアウトの**代わりに**なるので、
   [components/root-document.tsx](apps/web/components/root-document.tsx)の
   `RootDocument`をそのまま呼べます。`next.config.ts`へ
   `experimental.globalNotFound: true`が必要です（16.2時点では既定オフ）。
   CSSの読み込み（`@mojikumi/css`と`globals.css`）は、レイアウトを経由しない分
   このファイル側でも行います。

2. **`404.html`は1枚しか出ない。**
   `output: "export"`が書き出すのは`out/404.html`ひとつで、Vercelはこれを全ての
   未マッチURLに返します。`/en/`配下だけ別の404を返すには`vercel.json`側の細工が
   要りますが、**やらない**ことを推します。1枚で日英を賄います（後述）。

3. **`(ja)/not-found.tsx`は404.htmlにならない。**
   ルートグループ内の`not-found.tsx`は`notFound()`を呼んだときにだけ使われます。
   このサイトは動的ルートを持たないので出番がありません。作りません。

4. **サイトマップに載せない。**
   [lib/site.ts](apps/web/lib/site.ts)の`pagePaths`へ足すと`sitemap.xml`に出て
   しまいます。404の文言は`Dictionary`の`notFound`として持ち、`PageKey`には
   **足しません**。`<meta name="robots" content="noindex">`も付けます。

### 案A：落丁（推奨）

**考え方**：綴じ落ちた丁。本の側の言葉で「無い」を説明します。ただし言葉ではなく
**図版**にその役を持たせます（文言は後述）。

左に文字、右にガラスのカードという骨格はヒーローと共通ですが、**ヒーローではあり
ません**。見出しは節見出しの段まで下げ、縦の詰まりも`.page-intro`に寄せます。

```
┌─────────────────────────────────┬──────────────────┐
│ ERROR / 404                     │  ┌────────────┐  │
│                                 │  │ NOMBRE     │  │
│ この番号のページはありません        │  │            │  │
│                                 │  │    404     │  │  ← Shippori Mincho
│ URLが変わったか、まだ書かれていな   │  │            │  │     --display-2
│ いページかもしれません。            │  │ ─────      │  │
│                                 │  │ MOJIKUMI   │  │  ← 柱（はしら）
│ 01 はじめる  →                   │  └────────────┘  │
│ 02 ドキュメント →                 │                  │
│ 03 Playground →                 │                  │
│ 04 ホームへ戻る →                 │                  │
└─────────────────────────────────┴──────────────────┘
```

- 右のカードは`.hero-specimen`の系譜。中身が組み見本ではなく**ノンブル**（本文の
  ページ番号）です。数字を`--font-display`で組み、その下に細い罫と`MOJIKUMI`の柱
  を置きます。実在しない頁番号がそこに打たれている、という絵です。
- 行き先は`.index-mark`付きの4本。ボタンを2つ並べるより、目次らしく見えます。

### 文字サイズ

**このページの主役は数字で、見出しはその説明文です。** 大きさの順序をそう決めま
す。

| 要素 | トークン | サイズ | 根拠 |
| --- | --- | --- | --- |
| ノンブルの数字 | `--display-2` | 40–68px | ページ内で最大。図版として立たせる |
| 見出し`h1` | `--display-3` | 30–52px | `.section-heading h2`と同じ段 |
| 本文 | `--text-lead` | 15–18px | 他ページと同じ |

見出しを`--display-1`（ヒーロー）にしないのはもちろん、**`--display-2`にもしませ
ん**。`--display-2`は`/start/`や`/docs/`の`h1`が使っている段で、404がそれと同格に
見えるのは情報の重さと合いません。1段下げると型階層と実際の重要度が一致します。

当初案では数字を`--display-mark`（48–64px）としていましたが、見出しを
`--display-2`に置くと拮抗してどちらが図版か決まらなくなります。**数字を1段上げ、
見出しを1段下げる**組み合わせにします。

サイズが下がる結果、レイアウトも簡単になります。

- 見出しが1行で収まるので、`<br>`による二行組みをやめ、インラインの`<em>`で後半
  だけ臙脂にします（`.home-hero h1`のような`13.2cqw`の上限も不要）
- ヒーローの`min-height: min(780px, 100svh - 100px)`を外し、`.page-shell`の余白に
  収めます。404は画面を占有すべきページではありません

**推す理由**：既存の文法の中で完結し、説明が要らず、出版の比喩がプロダクトの主張
そのもの（Webの日本語を出版に近づける）と重なります。

### 案B：ノンブルだけ

画面中央に`404`だけを`--display-1`超のサイズで組み、上下に細い罫、下端に小さく行
き先を置く一枚。余白と罫だけで見せる案です。

- 強い。スクリーンショットが映える。
- ただし**行き先が弱くなる**。404は見た目より「どこへ行けるか」が仕事で、この案は
  そこを最小化してしまいます。トップページのヒーローとも文法が違うので、サイトの
  中で1枚だけ浮きます。

### 案C：空の約物

`『` と `』` を巨大に組み、その間に何も入れない。「ここに入るはずのものが無い」を
記号だけで言う案です。

- 最もブランドマークに近く、最も安い（DOMが3要素）。
- ただし**通じない可能性**が残ります。括弧の中身が空であることを「頁が無い」と読
  ませるのは、こちらの文脈を知っている人向けの冗談です。日本語話者以外にはまず
  届きません。

### 比較

| | 案A 落丁 | 案B ノンブル | 案C 空の約物 |
| --- | --- | --- | --- |
| 既存CSSの流用 | 高い | 低い | 中 |
| 行き先の見つけやすさ | 高い | 低い | 中 |
| サイト内での馴染み | 高い | 低い | 中 |
| 日英どちらでも通じるか | 通じる | 通じる | 日本語話者向け |
| 新規CSS量の目安 | 60行前後 | 70行前後 | 40行前後 |

**案Aを推します。** 案Cの空の約物は、案Aの右カードの装飾として一部を取り込めます
（ノンブルの数字を`『404』`として組む案も試す価値があります）。

### 文言案

まず語の選び方です。既存のコピーは`ページ`を29箇所で使い、`頁`は1度も出てきませ
ん。**出版の語彙は図版側**（`NOMBRE`のラベルと、ノンブルとして組んだ404）に持た
せ、**文章は平語のまま**にします。1つの見出しのためにレジスターを割る必要はなく、
比喩は絵が担保します。

見出しの候補は次の5つです。`lead`＋`<em>accent</em>`の2つ組で持ちます。

| | 日本語（`lead` ／ `accent`） | English | 性格 |
| --- | --- | --- | --- |
| **1 番号**（推奨） | この番号のページは ／ **ありません** | No page carries ／ **this number** | 右のノンブルカードと文章が同じ1つの仕事をする。URLを「番号」と言い換えるのは図版が支えるので通る |
| 2 組版 | ここには ／ **何も組まれていません** | Nothing is set ／ **on this page** | `組む`はこのプロダクト自身の動詞。ただし「ページはあるが空」とも読める |
| 3 印刷 | このページは ／ **刷られていません** | This page was ／ **never printed** | 静かで良いが、`この◯◯は、／〜ていません`の骨格が既出案とほぼ同じ |
| 4 目次 | 目次に ／ **このページはありません** | This page is not in ／ **the table of contents** | 下の行き先リストへ自然に繋がる。やや長い |
| 5 平叙 | そのページは ／ **見当たりません** | That page is ／ **not here** | 安全だが、どのサイトでも書ける。テーマが効かない |

**1を推します。** 移転したURLにも未作成のURLにも同じ理屈で当てはまり、右のカード
が「404という番号が打たれた、存在しないページ」を見せるので、文章と図版で説明が
一度で済みます。英語も比喩を輸入せずそのまま通ります。次点は4で、行き先リストへ
の導線が強くなる代わりにカードとの連動が弱まります。

`Dictionary`へ`notFound`を足します。
[content/content.test.ts](apps/web/content/content.test.ts)が日英の構造一致を強制
するので、`ja.ts`と`en.ts`へ同時に入れます。約物の後ろに空白を置けない規約にも注
意します。

| キー | 日本語 | English |
| --- | --- | --- |
| `title` | ページが見つかりません | Page not found |
| `eyebrow` | `ERROR / 404` | `ERROR / 404` |
| `headlineLead` | この番号のページは | No page carries |
| `headlineAccent` | ありません | this number |
| `lead` | URLが変わったか、まだ書かれていないページかもしれません。目次から探し直せます。 | The address may have changed, or the page may not be written yet. The table of contents is below. |
| `nombreLabel` | `NOMBRE` | `NOMBRE` |
| `runningHead` | `MOJIKUMI` | `MOJIKUMI` |
| `links[].label` | はじめる／ドキュメント／Playground／ホームへ戻る | Get started / Documentation / Playground / Back to home |

eyebrowは`ERROR / 404`のままにします。見出しが「番号」と言っている直上に数字が出
るので、案1と組むと噛み合います。出版寄りにするなら`NOMBRE / 404`。

`nombre`（ノンブル）は日本の組版用語ですが、語源はフランス語の`nombre`で、英語圏
の組版でも`folio`として同じものを指します。英語側は`FOLIO`にするか`NOMBRE`のまま
置くかを実装時に決めます。ラベルは装飾なので、揃えても構いません。

### 日英の出し分け

404.htmlは1枚なので、**両言語を書き出し、CSSでどちらを出すか決めます**。

`<head>`の同期スクリプト（テーマ復元と同じ位置）がパスを見て、`/en/`配下なら
`<html>`へ`lang="en"`と`data-not-found-locale="en"`を付けます。CSSは既定で英語側
を`display: none`にし、この属性が付いたときだけ日英を入れ替えます。**body**が構
文解析される前に属性が決まるので、片方が描かれてから差し替わることはありません。

JSが動かない場合は日本語のまま出ます。ヘッダーの言語切り替えは常に出ているので、
切り替わらなくても逃げ道があります。増えるDOMは404の本文だけで、ヘッダーとフッ
ターは日本語のまま共有します。

`<title>`だけは同じ手が使えません。Reactがハイドレーション時にメタデータの値へ
戻すため、スクリプトで書き換えても元に戻ります。**`404 — Mojikumi`**という、どち
らの言語でも読める題にしました。

### 追加・変更するファイル

| ファイル | 内容 |
| --- | --- |
| `apps/web/app/global-not-found.tsx` | 新規。`RootDocument`で包み、CSSと言語スクリプトを読み込む |
| `apps/web/components/pages/not-found.tsx` | 新規。表示部分。日英それぞれで1回ずつ呼ぶ |
| `apps/web/components/root-document.tsx` | `head`プロップを追加（レイアウトを持たないページ用） |
| `apps/web/next.config.ts` | `experimental.globalNotFound: true` |
| `apps/web/content/types.ts` | `Dictionary`へ`notFound`と`NotFoundLink`を追加 |
| `apps/web/content/{ja,en}.ts` | 文言 |
| `apps/web/app/globals.css` | `.not-found-*`を約150行追加 |

`lib/site.ts`は触りません（`pagePaths`へ足さない）。`NotFoundLink`の`page`は
`pagePaths`と同じキー名の文字列リテラル型にしてあるので、リンク先を書き間違えると
型検査で落ちます。

### 確認項目

| 項目 | 結果 |
| --- | --- |
| `out/404.html`が出て、`<html>`が1つだけ | 済 |
| `<html>`にフォントのクラスが付いている | 済 |
| 日英の出し分けとリンク先（`/en/start/`など） | 済 |
| ダーク／ライト、デスクトップ／375px | 済 |
| `noindex`と`404 — Mojikumi`の題 | 済 |
| `npm test`（67件）と`typecheck` | 済 |
| Vercelのプレビューで404ステータスが返る | **未**（デプロイ後） |
| スクリプトを切っても日本語側が読めて、リンクが押せる | **未** |

---

## 2. `cdn.mojikumi.jp`

### 「単純HTMLの方が逆に良いのか」への答え

**はい。単純HTMLのままが正解です。ただし今の中身のままではありません。**

分けて考えると、判断が2つ混ざっています。

1. **フレームワークを持ち込むか** → 持ち込まない。
2. **素のままにしておくか** → しておかない。設計された1ファイルにする。

フレームワークを入れない理由は4つあります。

| 理由 | 内容 |
| --- | --- |
| 役割が違う | ここは**ファイルの置き場**です。来る人は貼ったタグの出所を確かめに来た開発者か、審査のために配布元を見に来た人で、読み物を求めていません |
| 独立していないと意味がない | CDNは`mojikumi.jp`が落ちていても配信され続ける必要があります。トップページが本体サイトのCSSやWebフォントを参照した時点で、その独立性が嘘になります |
| 正規URLの二重化を避けた経緯 | [apps/cdn/README.md](apps/cdn/README.md)に書いたとおり、Projectを分けたのはサイト全体が`cdn.mojikumi.jp`からも見えるのを防ぐためです。ここに読み物を置くと、その判断を自分で崩します |
| ビルドの重さが配信の可用性になる | 今の`build.mjs`は文字列を1つ書き出すだけで、失敗のしようがありません。この軽さは配信元の性質として価値があります |

一方で、素のままで良い理由はありません。**今のトップは「作りかけ」に見えます**。
このページを見に来る動機の1つは「このスクリプトを信用してよいか」なので、そこで
UAの既定スタイルの`<pre>`が出てくるのは、機能面ではなく信用の面で損をしています。

目指すのは、**外部リクエスト0で、Mojikumiの色と組みに見えるHTML**です。テーマの実
体は色と細い罫と字間の効いたラベルなので、Webフォントなし・システムフォントだけで
も十分それらしくなります。むしろ「標準の範囲でここまでやる」という主張と一致しま
す。

（実装後の実測：CSSをインラインで抱えるため`index.html`は9.5KB、`404.html`は
5.8KBになりました。当初見積もりの3〜5KBは外していますが、外部リクエスト0という
性質のほうが重要なので、そのままにしています。）

### 載せるもの

| 区画 | 内容 | 理由 |
| --- | --- | --- |
| ブランド | `「` ＋ `Mojikumi CDN` ＋ ドメイン ＋ 言語の切り替え | ここが何かを3秒で言う |
| 貼る用のタグ | `<script>`1つ。そのままコピーできる形 | 来訪者の大半の用件 |
| 配信物の一覧 | パス／内容／キャッシュ方針を表で | `/v1/`と`/{version}/`の違いは表でしか伝わらない |
| 現在のバージョン | ビルド時点の`packages/mojikumi`のバージョンとnpmへのリンク | 「貼ったのはどれか」の答え |
| SRIハッシュ | `/{version}/`の`integrity`値 | 後述 |
| 導入手順への導線 | `mojikumi.jp/start/`へ1本 | 説明はここでは書かない |
| ライセンス | MITとリポジトリ | 審査で必ず見られる |

日英は**ページごとに分けます**（`/`と`/en/`）。当初は併記の予定でしたが、実装した
ものを読み返して変えました。併記だと同じ事実を2回ずつ述べることになり、実際の情報
量の倍の長さに見えます。日本語と英語が隣り合うと互いの対訳になり、どちらも直訳め
いた文章になります。`mojikumi.jp`が日本語＝ルート、英語＝`/en/`なので、同じ構造を
使えば2つのドメインで考え方が1つで済みます。

スクリプトによる出し分けはしません。リンク1本で足りますし、外部リクエスト0・JS任
意という前提とも噛み合いません。

404だけは併記のままです。外したパスを叩いた人がどちらの言語を読むのかは分からず、
本文を1行ずつに削れば、意味の大半はパスの一覧が担うからです。

### 自己検証としてのトップページ

このページからは、**同じオリジンが配信している`/v1/mojikumi.min.js`を読み込みます**。

```html
<script src="/v1/mojikumi.min.js" data-target="main"></script>
```

- 外部リクエストは増えません（同一オリジンで、しかも配信物そのもの）
- ページを開くこと自体が「バンドルが取得でき、パースでき、走る」ことの確認になり
  ます。デプロイ後に目視する先が1つで済みます
- JSが失敗してもページは読めます。これはライブラリの設計方針そのもの（CSS優先、
  DOMは補完）で、トップページがその実例になります

壊れているときにトップページも崩れるのは欠点に見えますが、**配信元としてはむしろ
そうあるべき**です。静かに壊れているより良い。

### SRI

`/{version}/`は1年間immutableで配信しているので、`integrity`値は安定します。
`build.mjs`がファイルのsha384を計算し、トップページに掲載します。

```html
<script
  src="https://cdn.mojikumi.jp/0.2.1/mojikumi.min.js"
  integrity="sha384-…"
  crossorigin="anonymous"
  data-target=".entry-content"
></script>
```

`Access-Control-Allow-Origin: *`は
[apps/cdn/vercel.json](apps/cdn/vercel.json)で既に付いているので、
`crossorigin="anonymous"`はそのまま通ります。`/v1/`は中身が変わるためSRIを付けら
れません。**その対比自体が`/v1/`と`/{version}/`の違いの一番わかりやすい説明**にな
るので、表の中で並べて見せます。

なお[ONBOARDING-PLAN.md](ONBOARDING-PLAN.md)の決定事項に「固定したい人向けに
`/{version}/`とSRIを併記」とありますが、SRI値の掲示はまだどこにもありません。ここ
が置き場所として妥当です。

### CDNの404

トップページと同じ体裁の`404.html`を出します。実利があります。

- 一番起きる失敗は綴り違いです（`/v1/mojikumi.js`、`/latest/mojikumi.min.js`、
  `/v1/`の直叩き）
- Vercelの静的配信は出力ルートの`404.html`を未マッチのパスに返します
- 中身は「そのパスは配信していません」＋**正しいパスの一覧**。ブラウザで直接開い
  た人はそこで解決できます

トップページの雛形を共有すれば追加コストはほぼありません。

### robots.txt

`index.html`の`noindex`は、そのHTMLを取得したクローラーにしか効きません。ドメイン
ごと索引から外したいので、`robots.txt`を書き出します。

```
User-agent: *
Disallow: /
```

`.js`や`.css`が検索結果に出る実害は小さいものの、本体サイトと配信元でURLが二重に
見えるのを避けるという当初の判断と一貫します。

### 実装の形

今の`build.mjs`はHTMLをテンプレートリテラルで組み立てています。中身が増えると、
インラインCSSまでJSの文字列の中に入り、編集もレビューも辛くなります。

**テンプレートをファイルに出します。**

| ファイル | 役割 |
| --- | --- |
| `apps/cdn/templates/index.html` | トップ（日本語）。`{{version}}` `{{channel}}` `{{integrityJs}}`などの差し込み口を持つ |
| `apps/cdn/templates/en.html` | 同じ内容の英語版。`/en/index.html`として書き出す |
| `apps/cdn/templates/404.html` | 同じ体裁の404（日英併記） |
| `apps/cdn/templates/page.css` | 3枚が共有するCSS。`{{style}}`として埋め込む |
| `apps/cdn/templates/robots.txt` | 固定 |
| `apps/cdn/build.mjs` | 読み込み、sha384を計算し、置換して`dist/`へ書く |

置換は正規表現1本で足ります。テンプレートエンジンは要りません。HTMLがHTMLとして
置いてあること自体が、この配信元の性格に合っています。差し込み口に対応する値が無
ければビルドを止めます。綴り間違いが`{{integrityJS}}`のまま配信されるより、落ちた
ほうがましです。

### やらないこと

| やらないこと | 理由 |
| --- | --- |
| Next.jsなどのフレームワーク | 上記のとおり |
| Webフォントの読み込み | 外部リクエスト0を崩す。システムの明朝で十分 |
| アクセス解析 | 配信元に計測を入れる理由がない。訪問者は開発者で、同意の扱いも増える |
| `mojikumi.jp`のCSSやOG画像の参照 | 独立性が嘘になる |
| バージョン一覧の自動生成 | ビルドは現行バージョンしか知りません。過去分を並べたいならnpmかGitHub Releasesへ送る |
| ビルド日時の埋め込み | 同じ入力から同じ出力が出なくなる。バージョンで足りる |

---

## 作業量の目安

| 項目 | 状態 |
| --- | --- |
| 404：文言（日英）と型 | 済 |
| 404：コンポーネントとCSS | 済 |
| 404：`global-not-found.tsx`とロケール差し替え | 済 |
| CDN：テンプレート4枚と`build.mjs`の改修 | 済 |
| CDN：SRIの計算と掲示 | 済 |
| CDN：`404.html`と`robots.txt` | 済 |

## CDNの確認項目

| 項目 | 結果 |
| --- | --- |
| `dist/`に`index.html` `en/index.html` `404.html` `robots.txt`が出る | 済 |
| 3枚とも外部の`<script>` `<link>` `<img>`を持たない | 済 |
| ブランドの文字ブロックがアイコンと同じ高さに収まる | 済（42px対41.6px） |
| 差し込み口が残っていない | 済 |
| SRIがブラウザの計算（SubtleCrypto）と一致する | 済 |
| ページが自分の`/v1/mojikumi.min.js`で組まれている | 済（`main`に`mjk`クラスが付く） |
| 外部リクエスト0 | 済 |
| ライト／ダーク | 済 |
| Vercelで存在しないパスが404ステータスで`404.html`を返す | **未**（デプロイ後） |

残るのは両方ともデプロイ後の確認だけです。`cdn.mojikumi.jp`のDNSとVercelのProject
設定は[apps/cdn/README.md](apps/cdn/README.md)にあります。
