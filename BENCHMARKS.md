# Benchmarks

再現できる条件で得られた計測だけを記録します。数値は実行した機械に依存するため、
結果そのものより**条件と、条件を変えたときの差**を残すことを目的にしています。

計測しているのは処理コストとレイアウトシフトの2つです。いずれもChromiumのみで、
FirefoxとWebKitはまだ揃えていません。

```bash
npm run build:packages && npm run build:browser
npm run measure:cost
npm run measure:shift
```

ブラウザは`PLAYWRIGHT_BROWSERS_PATH`から解決されます。別のバイナリを使うときは
`MOJIKUMI_CHROMIUM`に実行ファイルのパスを渡してください。

## 処理コスト

計測コードは[`scripts/measure-cost.mjs`](./scripts/measure-cost.mjs)です。

各セルを2回測ります。`auto`はこのブラウザの読者が実際に受け取る経路で、`full`は
ネイティブ実装を使わずフォールバックを強制した経路です。`full`は、CSS Text
Level 4の各プロパティを持たないブラウザで起きることの代役として測っています。

### 条件

| 項目 | 値 |
| --- | --- |
| ブラウザ | Chromium 148.0.7778.96（`text-spacing-trim`と`text-autospace`を実装） |
| 機械 | Apple Silicon / macOS |
| 本文 | サイトと同じJLREQの引用文（219字）の反復 |
| 幅 | 1280px |
| 書体 | `serif`（システム標準） |
| 時間 | 7回実行した中央値。フレーム内の処理はコールバックを計測して加算 |
| メモリ | 強制GCのあとのJSヒープ使用量。マウント前後の差 |

### 結果

| 本文 | 経路 | 初期処理 | 再評価 | DOM増加 | 生成要素 | ヒープ |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| 1,000字 | `auto` | **2.6ms** | **0.2ms** | **1** | 0 | 74KB |
| 1,000字 | `full` | 15.2ms | 5.0ms | 136 | 135 | 197KB |
| 10,000字 | `auto` | **5.5ms** | **0.2ms** | **1** | 0 | 88KB |
| 10,000字 | `full` | 45.6ms | 140.5ms | 1,243 | 1,242 | 250KB |
| 記事10本 | `auto` | **7.4ms** | **0.3ms** | **1** | 0 | 120KB |
| 記事10本 | `full` | 48.9ms | 148.5ms | 1,351 | 1,350 | 288KB |

### 読み方

- **ブラウザが実装していれば、DOMは1要素しか増えません。** その1つは注入する
  `<style>`で、本文には何も足していません。生成要素0は、字組みのすべてを標準CSS
  が引き受けたという意味です。設計上の主張がそのまま数値になっています。
- **その場合の処理は10,000字で5.5msです。** 内訳のほとんどはネイティブ対応の検出
  と行の計測で、本文の解析は走りません。再評価は0.2msで、実質的に無償です。
- **フォールバックが動くと初期処理は6〜9倍になります。** 約物1つにつきspanが1つ
  増えるため、10,000字で1,242要素。ヒープも2.5倍前後です。
- **フォールバック時の再評価は初期処理より高くつきます。** 10,000字で140.5msと、
  初期処理45.6msの3倍です。生成済みのspanを解いて`normalize()`してから解析を
  やり直すためで、本文が9倍になると再評価は28倍になります。ここは線形ではあり
  ません。幅の変更だけなら行の計測しか走らないため、この経路には入りません。
  入るのは本文が書き換わったときです。

`measure:cost`は、ネイティブ対応のあるブラウザで生成要素が1つでも出た場合に失敗
します。フォールバックが引き下がることが設計の前提なので、そこが崩れたら検出の
不具合として止めます。

## レイアウトシフト

スクリプトタグをどこへ置くかで、読者が本文を見たあとに行が動くかどうかが変わり
ます。ガイドが「headの中に置く」と案内している根拠です。

計測コードは[`scripts/measure-shift.mjs`](./scripts/measure-shift.mjs)です。
`/v1/`と同じバンドルを、40msの遅延を付けたローカルサーバーから配信します。

### 条件

| 項目 | 値 |
| --- | --- |
| ブラウザ | Chromium 148.0.7778.96 |
| 本文 | 上と同じ引用文。8段落と40段落 |
| 幅 | デスクトップ1280px、モバイル390px |
| 書体 | `serif`（システム標準） |
| 計測 | `layout-shift`エントリーの合計（`hadRecentInput`を除く） |

### 結果

| 配置 | デスクトップ1280 | モバイル390 |
| --- | ---: | ---: |
| 貼らない | 0 | 0 |
| `head`の中 | **0** | **0** |
| `body`の終わり | **0** | **0** |

段落数（8 / 40）で結果は変わりませんでした。

以前Chromium 141.0.7390.37で測ったときは、`body`の終わりに置いた場合だけモバイル
390pxで**0.033**が出ていました。本文の高さが3383pxから3111pxへ縮み、その差が一度に
動いていたためです。148では貼らない場合の高さが最初から3111pxで、Mojikumiを貼っても
変わりません。**ブラウザ側が追いついて、差そのものが消えました。**

141の数値は当時の記録として残しています。手元では再現していません。

### 読み方

- **`head`に置けばシフトは発生しません。** CSSが最初のペイントより前に届くため、
  読者が見る前に行が確定します。これは141でも148でも同じでした。
- **`body`の終わりに置いたときの危険は、ブラウザに依存します。** 148で0なのは、
  この検体でMojikumiが足すものが無くなったからです。フォールバックが動くブラウザ
  では事情が違うため、`head`を勧める理由は消えていません。
- **数値が0でも、タグが動いていないだけかもしれません。** 計測は`.mjk`が付いた
  要素の数も記録し、貼ったはずのページで0だった場合は失敗します。

## まだ計測していないもの

- FirefoxとWebKit。フォールバックが実際に動くのはこちらなので、`full`の列が
  代役ではなく実測になります
- 書体を変えた場合の差。`halt`や`chws`の有無で結果が動く見込みです
- 縦組み
