# Benchmarks

再現できる条件で得られた計測だけを記録します。数値は実行した機械に依存するため、
結果そのものより**条件と、条件を変えたときの差**を残すことを目的にしています。

現在記録しているのはレイアウトシフトのみです。処理時間、DOM増加量、メモリは
計測条件を確定していないため、まだ数値を出しません。

## レイアウトシフト

スクリプトタグをどこへ置くかで、読者が本文を見たあとに行が動くかどうかが変わり
ます。ガイドが「headの中に置く」と案内している根拠です。

```bash
npm run build:packages && npm run build:browser
npm run measure:shift
```

計測コードは[`scripts/measure-shift.mjs`](./scripts/measure-shift.mjs)です。
`/v1/`と同じバンドルを、40msの遅延を付けたローカルサーバーから配信します。

### 条件

| 項目 | 値 |
| --- | --- |
| ブラウザ | Chromium 141.0.7390.37 |
| 本文 | サイトと同じJLREQの引用文。8段落と40段落 |
| 幅 | デスクトップ1280px、モバイル390px |
| 書体 | `serif`（システム標準） |
| 本文の指定 | `font-size: 17px`、`line-height: 2`、`max-width: 38rem` |
| 計測 | `layout-shift`エントリーの合計（`hadRecentInput`を除く） |

### 結果

| 配置 | デスクトップ1280 | モバイル390 |
| --- | ---: | ---: |
| 貼らない | 0 | 0 |
| `head`の中 | **0** | **0** |
| `body`の終わり | 0 | **0.033** |

段落数（8 / 40）で結果は変わりませんでした。シフトは1回だけで、記事が長くても
増えません。

### 読み方

- **`head`に置けばシフトは発生しません。** CSSが最初のペイントより前に届くため、
  読者が見る前に行が確定します。
- **`body`の終わりに置くと、狭い画面でだけずれます。** モバイルでは本文の高さが
  3383pxから3111pxへ縮み、その差が一度に動きます。Core Web Vitalsの「良好」と
  される0.1は下回りますが、0にはなりません。
- **デスクトップでは`body`の終わりでもシフトしません。** 幅が広いと行数が変わら
  ないためで、環境によって変わる可能性があります。狭い画面で測ってください。

計測スクリプトは、`head`配置でシフトが出た場合に失敗します。ガイドが案内している
置き方が無害であることを、後からの変更で壊さないためです。

## まだ計測していないもの

サイトの[Benchmarks](https://mojikumi.jp/benchmarks/)に挙げている次の指標は、
条件を固定できていないため未計測です。

- 初期処理と再評価の時間
- DOM増加量の環境ごとの比較
- メモリ使用量
- Chromium以外のブラウザ（Firefox、WebKit）
