# Mojikumi v0.1 Specification

この文書は実装判断を再現可能にするための規範的なサブセットです。組版上の根拠は
JLREQとCSS Textを参照し、Mojikumi固有の処理は保守的な互換レイヤーに限定します。

## 対象

- 横書き日本語
- 連続約物の重複アキ
- 折り返された行頭の開き約物
- 行末の閉じ約物・句読点
- 和文とLatin文字・数字の境界
- CSS標準機能のプリセット

縦書き、ルビの高度な配置、全文字種のアキ量、追込み・追出し、形態素解析は
v0.1の対象外です。

## 文字クラス

| 内部クラス | 初期対象 |
| --- | --- |
| `opening` | `（［｛〈《「『【〔〖〘〚〝` |
| `closing` | `）］｝〉》」』】〕〗〙〛〟` |
| `comma` | `、，` |
| `period` | `。．` |
| `middle` | `・：；` |
| `question` | `？！⁉⁈` |
| `ideograph` | Han、Hiragana、KatakanaのScript_Extensionsと反復記号 |
| `latin` | Unicode Script=Latin（全角形を除く） |
| `numeric` | Unicode Number（全角形を除く） |
| `space` | Unicode空白 |
| `other` | 上記以外 |

判定は上から順に、約物表 → 空白 → 全角形 → その他の約物・記号 → `ideograph`
→ `latin` → `numeric`、の順です。

`ideograph`にScriptではなくScript_Extensionsを使うのは、長音符`ー`が
Script=Commonのためです。Scriptで見ると`other`に落ち、`コーヒーAPI`や
`メニュー100円`で和欧間のアキが入りませんでした。Chromiumはここに0.125emを
入れるので、フォールバックも合わせます。

全角形（U+FF01–FF60、U+FFE0–FFE6）は`latin`・`numeric`から外します。`Ａ`や`１`は
Unicodeの分類こそLatinとNumberですが、CSS Text 4は英数字側から全角形を除外し、
Chromiumも実測で前後に何も入れません。

### 調整しない約物

`〜` `…` `‥` `―`と半角約物`｡｢｣､･`は`other`のままにします。Chromium 148で測ると、
`〜…‥―`は隣に何が来ても1emのまま詰まらず、半角約物はもともと0.5emで削る余白が
ありません。**余白を持たない字を半em詰めれば、隣の字に重なります。**

`〝〟`は逆で、`「〝引用〟」`のように括弧と隣接すると`text-spacing-trim: normal`が
0.5emへ詰めます。`『』`と同じ扱いなので、開き・閉じとして表に加えました。

入力は`Intl.Segmenter`の書記素クラスタ単位で分割します。結合文字、異体字
セレクタ、ZWJを含む絵文字を分断しません。

## ペアルール

境界の調整量はemで表します。v0.1の汎用フォールバックは、全角約物が持つ
半em相当のアキが隣接して重複するケースだけを`-0.5em`調整します。

| 左 | 右 | 調整 |
| --- | --- | ---: |
| opening | opening | -0.5em |
| closing | closing | -0.5em |
| closing | opening | -0.5em |
| comma | opening | -0.5em |
| period | opening | -0.5em |
| closing | comma | -0.5em |
| closing | period | -0.5em |

中点・疑問符・感嘆符は一律に詰めません。フォントプロファイルがない環境では
この表より積極的な調整を行いません。

## 行コンテキスト

`paragraph-start`と`wrapped-line-start`を区別します。

- ブロック内に先行する文字がなければ`paragraph-start`
- 先行文字があり、Rangeの矩形が別行なら`wrapped-line-start`
- どちらも行頭なので、開き約物を`-0.5em`調整する
- 区別は`data-mjk-context`に残す

行末も現在文字と後続文字のRange矩形が別行かどうかで判定します。行頭と同じく
探索は同一ブロック内に閉じます。次の段落は必ず別行に来るため、ブロックをまたい
で比べると、行のどこにあっても段落末尾の閉じ約物が行末と判定されてしまいます。
ブロック内に後続文字がなければ行末調整は行いません。スクロールイベントでは
再測定しません。

### 測定は自分の結果に照らして検証する

調整は自分の根拠を壊すことがあります。行末の閉じ約物を半em詰めると次の文字が
その行に入る余地が生まれ、ブラウザがそれを取ると、行末と判定した約物は行の
途中で負のマージンを持ったまま、繰り上がってきた文字に重なります。しかも
調整を外せば文字は元の行へ戻るため、2つの状態を往復して収束しません。

そこで測定は次の順序で行い、検証では**調整を取り消すだけで、付け直しません**。

1. 候補のクラスをすべて外す
2. すべての候補を測る（読み取りのみ）
3. すべての候補にクラスを付ける（書き込みのみ）
4. 付けた候補を測り直し、根拠が消えていれば取り消す

取り消しは単調なので必ず停止し、行き着く先は「調整なし、ブラウザが組んだ
ままの行」という安全な状態です。

## 和欧間

`ideograph`または和文約物と、`latin`または`numeric`の境界に`0.125em`の
視覚的アキを入れます。ネイティブの`text-autospace: normal`が利用可能なら
ブラウザへ委譲します。フォールバックは空の疑似要素を使うため、コピーされる
文字列へ空白を追加しません。

## 行末と両端揃え

行末の閉じ約物・句読点は、字面の右にある二分アキを削って版面の右端へ届かせる
調整です。**この調整が意味を持つのは両端揃えのときだけ**で、行末を揃えない
組み方では句読点の右に何も無いため、削っても字は動きません。

Chromium 148での実測では、219字の本文を幅18〜34emの17通りで組んだとき、
左揃えで行末調整が字を動かしたのは2通りだけで、その2通りはいずれも
「行の折り返し位置が変わってしまった」ケースでした。両端揃えでは7通りで
字が動きました。

このため、行末をJSフォールバックで扱うのは`justify: true`のプリセットに
限ります。それ以外では行末をブラウザに委ね、`text-spacing-trim: trim-both`が
使えるようになればCSSだけで揃います。

## 機械が書いた連続

日本語の行はほとんどどの文字間でも折れるため、URLや環境変数名のような
Latinの連続は、段落の中で唯一「折れない塊」になります。残り幅に入らなければ
ブラウザは塊ごと次行へ送り、両端揃えの行はその手前で引き伸ばされます。
Chromium 148・幅24emの実測で、URLの手前の行は字間4.4em、環境変数名では
7.3emまで開きました。

### 何を連続とみなすか

`latin` / `numeric` / `other`が16文字以上続き、その中に`/:_=&?#%@~`の
いずれかを含むものだけです。**記号を要求するのは、機械が書いた文字列に
限るため**です。散文には折るべき空白があり、英単語を音節の途中で折るほうが、
避けようとしたアキより読みにくくなります。そちらは両端揃えの検査に任せ、
語を壊すのではなく段落の両端揃えを降ろします。

### どこで折るか

連続の中で折ってよいのは、次の記号の**直後**だけです。

| 折れる | `/` `:` `_` `=` `&` `?` `#` `@` `~` |
| --- | --- |
| 折れない | `%` |

連続と判定した根拠がこれらの記号である以上、折る位置にも同じ記号を使えば
足ります。`/`はパスの区切り、`?`と`#`はパスの終わり、`&`は引数の切れ目で、
そこで折れば行末はアドレス自身の切れ目に一致し、**ドメイン名は割れません**。
ドメイン名を途中で折ると、読者にはそう綴られている名前と区別できません。

`%`だけは外します。直後の2桁は1つのエスケープであり（RFC 3986 §2.1）、
その間で折るのはアドレスではなく文字を割ることになります。

記号の直後に折るのは、記号が直前の語に残るためです。行頭に`/`だけが
落ちることも、行末が別の記号に見えることもありません。

### どう折るか

該当位置へ`<wbr>`を挿入します。要素であって文字ではないため、`textContent`
にも、読者がコピーした文字列にも、読み上げにも現れません。文字列を変更
しないという不変条件を保ったまま、通常の改行機会として扱われます。

区切りと区切りのあいだが1行に入りきらない場合だけ、最後の手段として
`overflow-wrap: anywhere`が任意位置で折ります。

この方式では、記号を含まない先頭区間が「次行なら丸ごと入る」場合に塊ごと
送られ、手前の行が伸びます。任意位置で折る従来の挙動はそれを防いでいました
が、防ぐ手段がドメイン名を割ることでした。伸びが過大な場合は両端揃えの検査が
段落ごと降ろすため、最悪の場合は塞がっています。

## プリセット

印刷の体裁にどこまで寄せるか、という一本の軸に3つ並べます。

| 値 | minimal | article | book |
| --- | --- | --- | --- |
| 役割 | 標準CSSで届く範囲の妥協 | Mojikumiが本来やること | 書籍の体裁 |
| 連続約物 | ✓ | ✓ | ✓ |
| 行頭調整 | ✓ | ✓ | ✓ |
| 和欧間 | ✓ | ✓ | ✓ |
| 両端揃え | — | ✓ | ✓ |
| 行末調整 | 条件付き | ✓ | ✓ |
| 段落字下げ | — | — | 1em |
| ぶら下げ | — | — | ✓ |
| 見出し文節改行 | — | — | — |

既定は`minimal`です。行末調整には両端揃えが要り、両端揃えには
`text-spacing-trim: trim-both`が実装されるまでDOMフォールバックが要ります。
**何も指定しなかったページを、その負担に署名させない**というのが既定を妥協に
しておく唯一の理由です。ブラウザが追いつけば`article`は無償になり、そのとき
既定を`article`へ移します。

`minimal`の行末が「条件付き」なのは、ブラウザがCSSだけで揃えられるなら
その恩恵は受けるという意味です。フォールバックを起動しないだけで、結果を
拒んではいません。

`book`は`article`に字下げとぶら下げを足したものです。差はその2つだけで、
どちらも修飾子なので上書きできます。

### 退役した名前

| 旧 | 解決先 |
| --- | --- |
| `web` | `minimal` |
| `editorial` | `minimal`（見出しは`headingBreak`修飾子、または`mjk-heading`クラス） |
| `native` | `minimal` + `precision: "native"` |

`native`は「標準CSSだけ」という意味で、それは`precision`が言うことでした。
プリセットの`fallback`フラグは廃止し、`precision`に一本化しています。

## 修飾子

段落字下げと両端揃えは、日本語組版の正誤ではなく、そのページをどう設計するか
の判断です。プリセットに固定せず、`indent`と`justify`で上書きできます。

| 指定 | 意味 |
| --- | --- |
| 省略 | プリセットの判断に従う |
| `indent: false` | 字下げなし |
| `indent: "2em"` | 量を指定（CSSの`--mjk-paragraph-indent`でも同じ） |
| `justify: false` | 両端揃えをやめる。**行末調整も止まる** |
| `justify: true` | 両端揃えにする |
| `hanging: true` | 約物を版面の外へぶら下げる（WebKitのみ） |
| `headingBreak: true` | 見出しを文節で折る |

修飾子は解決済みプリセットへ畳み込みます。以降の判定はすべてプリセットを見る
ので、上書きの参照漏れが起きません。ルートに付くクラスは、プリセット既定と
**食い違うときだけ**です。既定を明示しても意味がないうえ、`text-align: start`
をすべての左揃えルートに書くことになり、記事内で中央揃えにした引用が巻き添えに
なります。

## DOM不変条件

- 文字列を変更しない
- 生成要素は`data-mjk-generated`で識別できる
- 同じ要素への`refresh()`を繰り返しても結果が増殖しない
- `destroy()`で生成要素とルート属性を復元する
- 除外要素と`data-no-mojikumi`の内部を処理しない
